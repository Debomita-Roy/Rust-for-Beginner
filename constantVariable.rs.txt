fn main() {
    const SECONDS_IN_MINUTE: u32 = 60;
    //const SECONDS_IN_MINUTE: u32 = 100;// This line will throw an error because unlike variables , the value of const cannot be changed
    println! ("{}", SECONDS_IN_MINUTE );
    
    
}